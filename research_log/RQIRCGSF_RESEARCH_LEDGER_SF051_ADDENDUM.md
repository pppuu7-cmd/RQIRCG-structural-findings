# RQIRCGSF research ledger — SF051 addendum

Date: 2026-09-15

## Gate

`SF051_ALGEBRAIC_CURVATURE_CUBIC_QUOTIENT_PREOUTCOME_GATE`.

## Prospective chronology

Preregistration:

`49eb8d22c4d69460306989c50b4ed32d3acc02ee`.

Executed symbolic script:

`125c8efc46fe02c4b5c0191c1abc641a6b3eafe0`.

Canonical exact raw result:

`b792beee6432fea224c661f4ee3121d1dfc02f27`.

Terminal:

`6dab7d8bcf8ba6fbdeb49efa5f22a652b89551dc`.

## Classification

`PASS_ALGEBRAIC_QUOTIENT_SCOPED`.

Secondary:

`ALGEBRAIC_RICCI_REDUNDANCY_REMOVABLE_BY_EXACT_TT_GRAM_QUOTIENT_SCOPED`.

Qualification:

`DERIVATIVE_SIX_DERIVATIVE_REDUNDANCIES_NOT_YET_INCLUDED`.

## Exact result

Frozen basis `[C3,S3,SSC]` has Gram matrix

`[[95/768,-35/768,7/512],[-35/768,5/192,-1/128],[7/512,-1/128,15/1024]]`.

The redundant `[S3,SSC]` subspace has exact determinant `21/65536` and rank 2.

The exact essentialized algebraic tensor is

`T_C3_perp = T_C3 + (7/4) T_S3`.

Its norm is `45/1024` and it is exactly orthogonal to both frozen redundant tensors.

The normalized projector returns exactly 1 on C3 and 0 on S3 and SSC.

Naive single-template contaminations were `-7/19` and `21/190`, respectively.

All scalar-R pure linearized-curvature cubic controls vanish because `R^(1)=0` on the frozen TT legs.

## Frontier effect

SF050's generic off-shell quotient blocker is narrowed.

Algebraic curvature-cubic Ricci redundancy is closed in the declared TT/symmetric-point subspace.

Exact remaining quotient problem:

`DERIVATIVE_REDUNDANT_COMPLETION_OF_ESSENTIAL_C3_FLUCTUATION_PROJECTOR`.

Alternative future route: a direct physical on-shell dynamical flow could bypass off-shell EOM redundancies if a legitimate Lorentzian/complex on-shell FRG vertex construction is supplied.

## Selector accounting and locks

`R_ASGS_TRUNCATION=1`.

`R_ASGS_PHYSICAL=UNDEFINED_MAP_NOT_CLOSED`.

No physical `b`, no projected FRG beta function, no background/fluctuation equality, no regulator independence, no quantum `chi_ABC`, no parent promotion, no new-physics/full-QG claim.
