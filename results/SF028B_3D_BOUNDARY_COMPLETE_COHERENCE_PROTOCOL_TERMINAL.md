# SF028B — 3D boundary-complete finite-time closed coherence protocol — TERMINAL

Date: 2026-09-15
Preregistration: `521ee723d66ff289f447c5474f7715b3fdd16dc3`.
Script: `scripts/sf028b_checks.py`, commit `46a6743f7f718b07e4fa452f53ebb61b31f8e9ae`.
Raw: `results/raw/SF028B_CHECKS.json`, commit `16cffa02797e9616c856d580445d994e836bd90a`.
Derivation/source authority: `research_log/SF028B_COHERENCE_DERIVATION_AND_AUTHORITY_NOTES.md`, commit `8f04567d533fcbadd81c8a593322960033439c0e`.

## RESULT / CLASSIFICATION

`PASS_BOUNDARY_COMPLETE_KNOWN_PHYSICS_COHERENCE_BASELINE_SCOPED`

Secondary structural classifications:

`FINITE_R_NEWTONIAN_APPARATUS_CONNECTED_BASELINE_NONZERO_SCOPED`

`SELF_CONSISTENT_NEWTONIAN_COHERENCE_PULLBACK_NONZERO_SCOPED`

`SAME_PROTOCOL_EIH_MINUS_NEWTONIAN_1PN_COHERENCE_DERIVED_SCOPED`

No quantum-gravity matching contribution is included. No quantum `chi_ABC` is computed.

## Physical object

The frozen normalized protocol is:

`|+++> x 3D Gaussian motional state`

`-> COM-closed branch-conditioned translations U_prep`

`-> all-body free evolution U_X(T)`

`-> U_read=U_prep^dagger`

`-> trace four motional systems`

`-> informationally complete three-qubit tomography`.

For each branch `s` relative to `000`,

`K_s^X(T)=rho_q^X[s,000]/sqrt(rho_q^X[s,s]rho_q^X[000,000])`.

The physical connected observable is

`C3^X = Log K_111^X-Log K_110^X-Log K_101^X-Log K_011^X+Log K_100^X+Log K_010^X+Log K_001^X`,

with the continuous logarithm from `T=0`.

Define

`Theta3^X=Im C3^X`,

`Gamma3^X=-Re C3^X`.

This is a reduced-density-matrix/tomography object. No open action is promoted.

## Domain repair PASS

The SF028 one-dimensional Gaussian defect is absent.

In 3D:

- `1/r` is locally integrable because `r^2 dr/r ~ r dr`;
- `1/r^2` repeated-label 1PN singularities are locally integrable because `r^2 dr/r^2 ~ dr`;
- distinct `1/(r_AB r_AC)` triple products are locally integrable in the six relative dimensions near simultaneous coincidence.

The frozen separated Gaussian centers therefore define a valid local point-particle weak-field packet object for the declared leading-semiclassical gate.

Classification:

`PASS_3D_PACKET_DOMAIN_SCOPED`.

## Short-time physical coherence theorem

At leading semiclassical/narrow-packet order, translate each branch back to the common reference frame and write

`H_s=K+V_s+g_s.x+...`,

`g_s=grad V(q_s)`.

The linear-force Magnus expansion terminates because the relevant commutator is a c-number. With

`A_s=g_s^T M^-1 g_s`,

the branch-reference coherence phase is

`arg K_s=-(V_s-V_0)T/hbar +(A_s-A_0)T^3/(12hbar)+...`.

Therefore

`Theta3^N(T)=-(T/hbar) Delta3 V_N +(T^3/(12hbar)) Delta3 A_N + ...`.

This `1/12` coefficient is a physical echo/coherence coefficient for the frozen preparation/recombination/readout. It is intentionally not the SF026 open released-action coefficient `1/3` or fixed-return coefficient `-1/24`.

This directly confirms the imported warning:

`OPEN_ACTION != MEASURED/REDUCED COHERENCE`.

## Finite-R Newtonian result

For the frozen equal source masses, `M_D=5m`, `R=100 ell`, branch geometry

`A=a ell`, `B=(4+2b)ell`, `C=(10+3c)ell`,

and COM-closed apparatus center,

exact rational arithmetic gives

`Delta3 V_N = -3.441209917267863e-7 * G m^2/ell`.

Thus the physical raw connected coherence phase contains the finite-R ordinary-Newtonian apparatus term

`Theta3_N,apparatus = +3.441209917267863e-7 * G m^2 T/(hbar ell)`.

This is an apparatus/COM closure effect. It is not a three-body Newtonian gravitational vertex.

Classification:

`FINITE_R_NEWTONIAN_APPARATUS_CONNECTED_BASELINE_NONZERO_SCOPED`.

## Self-consistent Newtonian force/coherence result

The same exact branch calculation gives

`Delta3 A_N = -1.3308528151997442e-4 * G^2 m^3/ell^4`.

Hence

`Theta3_N,feedback = -1.1090440126664535e-5 * G^2 m^3 T^3/(hbar ell^4)`

at the retained short-time order.

This is the boundary-complete reduced-coherence analog of the SF026 trajectory-pullback phenomenon. It is generated by ordinary pairwise Newtonian dynamics after self-consistent evolution/readout; no new cubic gravitational coupling is present.

Classification:

`SELF_CONSISTENT_NEWTONIAN_COHERENCE_PULLBACK_NONZERO_SCOPED`.

## Leading connected visibility cumulant

For the frozen isotropic packet width `sigma=ell/100`,

`Gamma3^N=(sigma^2 T^2/(2hbar^2)) Delta3 Q + O(T^4)`

with

`Delta3 Q = -1.3308490057392498e-4 * G^2 m^4/ell^4`.

Therefore

`Gamma3^N = -6.654245028696249e-9 * G^2 m^4 T^2/(hbar^2 ell^2) + O(T^4)`.

This is a connected logarithmic visibility cumulant; its sign is not a positivity statement or a decoherence rate.

## EIH power-counting result

All branch mean velocities are zero at preparation. Newtonian motion gives `v=O(GT)`.

Consequently:

- `G v^2 T/c^2 = O(G^3T^3/c^2)` for velocity-dependent EIH terms;
- `v^4 T/c^2 = O(G^4T^5/c^2)`;
- pulling the static `O(G^2/c^2)` potential along the Newtonian displacement gives `O(G^3T^3/c^2)`;
- inserting the 1PN trajectory correction into the Newtonian action/coherent-state phase also begins beyond the retained `O(G^2T/c^2)` order.

Therefore the frozen same-protocol EIH-minus-N comparator through `O(G^2T/(hbar c^2))` receives only the initial static EIH contribution.

This is a prospective power-counting omission, not an outcome-driven deletion of velocity terms.

## Finite-R 1PN known-physics comparator

The full all-body static EIH ordered sum at `R=100 ell` gives

`Delta3 V_static^(1PN)=+0.002070114091375611 * G^2 m^3/(c^2 ell^2)`.

Thus

`Delta_1PN Theta3(T)`

`= Theta3^EIH-Theta3^N`

`= -0.002070114091375611 * G^2 m^3 T/(hbar c^2 ell^2)`

`+ O(G^3T^3/(hbar c^2),c^-4,eta^2 epsilon_PN)`.

Classification:

`SAME_PROTOCOL_EIH_MINUS_NEWTONIAN_1PN_COHERENCE_DERIVED_SCOPED`.

The comparator uses the same state, apparatus, finite R, preparation, evolution time, inverse recombination and tomography basis in both models.

## Source-only decoupling controls

For the same source geometry in the formal `R/ell -> infinity` limit:

`Delta3 V_N -> 0`,

`Delta3 A_N -> -82/616005 * G^2 m^3/ell^4`,

`Delta3 V_static^(1PN) -> (2/945) G^2 m^3/(c^2 ell^2)`.

The `-82/616005` value reproduces the exact SF026 source-only force-pullback result.

The `2/945` coefficient is the source-only 1PN coefficient for the **SF026 geometry**. It differs from SF022's `13/2520` because SF022 froze a different branch geometry.

The executed `R/ell=50,100,200,1000` ladder approaches these values.

At `R=100 ell`:

- the `A` coefficient is already within about `2.29e-4` fractionally of the source-only limit;
- the full static 1PN coefficient still differs by about `2.19e-2`.

Thus finite-R 1PN apparatus terms are materially larger than the Newtonian force-decoupling error would suggest.

## Delete-one-label controls

Setting any one source displacement amplitude to zero gives exactly

`Delta3 V_N=Delta3 A_N=Delta3 V_static^(1PN)=Delta3 Q=0`.

All three labels are therefore required by every reported connected coefficient.

## Baseline hierarchy / calibration fact

Introduce

`epsilon_PN=Gm/(c^2 ell)`.

The ratio of the finite-R Newtonian apparatus `O(T)` coefficient to the 1PN `O(T)` coefficient is

`1.6623286279748693e-4 / epsilon_PN`.

The ratio of the Newtonian force-pullback `T^3` term to the 1PN term is

`0.005357405262284278 * tau^2/epsilon_PN`,

where `tau=T/sqrt(ell^3/(Gm))`.

Therefore ordinary apparatus closure and source motion can dominate the raw connected coherence in sufficiently weak-field/long-enough regimes. The correct theory discriminator is the complete same-protocol baseline comparison, not the observation of a nonzero connected phase alone.

This is a structural calibration statement, not an experimental-feasibility claim.

## Finite-size applicability

Primary PN/EFT authority supports the standard effacement statement that spinless finite-size/tidal operators enter well beyond 1PN in the standard compact-object PN counting:

- Goldberger & Rothstein, arXiv:hep-th/0409156;
- Schäfer & Jaranowski, arXiv:1805.07240 and the historical EIH fluid-ball derivations reviewed there.

Accordingly, the point-particle/monopole gravitational result is consistent through the retained 1PN order for the frozen nonspinning spherical-body applicability class.

This does not model material branch-generation stresses, deformation during control pulses, engineering noise, or apparatus implementation. No laboratory-feasibility promotion is made.

Classification:

`PASS_1PN_MONOPOLE_EFFACEMENT_APPLICABILITY_SCOPED`.

## Boundary/readout audit

Because the authoritative output is the final reduced qubit density matrix after `U_read`, a coordinate/canonical total derivative cannot be promoted by inspecting an isolated propagation action. Endpoint/control transformations must be applied to the full state/readout object.

This is consistent with standard interferometer bookkeeping in which propagation, interaction/control and separation/readout contributions jointly define the observable (external sanity comparator: Dimopoulos et al., arXiv:0802.4098).

Classification:

`PASS_BOUNDARY_COMPLETE_REDUCED_COHERENCE_OBJECT_SCOPED`.

## New structural fact

A boundary-complete connected observable has now been constructed in a controlled known-physics scope, and it makes the earlier baseline warning quantitative:

`RAW_CONNECTED_COHERENCE = FINITE_R_NEWTONIAN_APPARATUS + NEWTONIAN_SELF_CONSISTENT_PULLBACK + 1PN_KNOWN_PHYSICS + ...`

Therefore:

`NONZERO_CONNECTED_SIGNAL != NEW_THREE_BODY_GRAVITATIONAL_VERTEX`.

The source-only static 1PN term is not the complete finite-time instrument baseline.

## What remains unestablished

SF028B does not establish:

- exact all-time packet dynamics;
- a full quantum EIH channel beyond leading semiclassical order;
- material/control-pulse engineering;
- realistic detector noise;
- a successor quantum matching law;
- a GR-versus-quantum-gravity discriminator;
- a measured laboratory value;
- new physics;
- quantum `chi_ABC`.

## Exact next admissible direction

The operational track has crossed the object-definition boundary. The next operational gate should be a **robustness / observable-baseline hierarchy gate**, not a new connected coefficient search:

`SF029_FINITE_TIME_COHERENCE_BASELINE_ROBUSTNESS_PREOUTCOME_GATE`.

It should prospectively vary only physically motivated nuisance families (finite R, packet width, allowed spherical finite-size scale, and short-time window) and test whether the same-protocol Newtonian/1PN separation is stable without post-hoc geometry selection.

The independent theory track remains blocked on

`GENUINELY_NEW_MICROSCOPIC_MATCHING_PRINCIPLE_WITH_EXPLICIT_PHYSICAL_MAP_REQUIRED`.

Do not mix SF029 operational robustness with the SF025/SF027 quantum-law matching problem.