# SF051 — algebraic curvature-cubic redundant quotient — TERMINAL

Date: 2026-09-15
Preregistration: `49eb8d22c4d69460306989c50b4ed32d3acc02ee`.
Executed symbolic script: `scripts/sf051_algebraic_cubic_quotient.py`, commit `125c8efc46fe02c4b5c0191c1abc641a6b3eafe0`.
Canonical raw exact result: `results/raw/SF051_ALGEBRAIC_CUBIC_QUOTIENT.json`, commit `b792beee6432fea224c661f4ee3121d1dfc02f27`.

## RESULT / CLASSIFICATION

`PASS_ALGEBRAIC_QUOTIENT_SCOPED`.

Secondary structural result:

`ALGEBRAIC_RICCI_REDUNDANCY_REMOVABLE_BY_EXACT_TT_GRAM_QUOTIENT_SCOPED`.

Mandatory qualification:

`DERIVATIVE_SIX_DERIVATIVE_REDUNDANCIES_NOT_YET_INCLUDED`.

No physical SF025 matching coefficient is selected.

## Frozen basis

Essential target:

`T_E = T[C_{rho sigma}^{mu nu} C_{mu nu}^{alpha beta} C_{alpha beta}^{rho sigma}]`.

Redundant algebraic controls:

`T_R1 = T[S_mu^nu S_nu^rho S_rho^mu]`,

`T_R2 = T[S^{mu nu} S^{rho sigma} C_{mu rho nu sigma}]`.

Scalar-containing cubic controls were frozen as

`R^3`, `R S^2`, `R C^2`.

At the frozen linearized-TT level around flat space, `R^(1)=0` identically for every basis leg, so those scalar-R cubic controls vanish exactly in the declared `curvature^(1)^3` sector.

## Exact frozen kinematics

Real Euclidean symmetric point:

`p_i^2=1`,

`p_i.p_j=-1/2`, `i != j`,

`p1+p2+p3=0`.

Each external leg uses the same orthonormal five-dimensional transverse-traceless tensor basis as SF050.

## Exact Gram matrix

In basis `[T_E,T_R1,T_R2]`:

`G =`

`[[95/768,  -35/768,   7/512],`
` [-35/768,   5/192,  -1/128],`
` [  7/512,   -1/128, 15/1024]]`.

The redundant sub-Gram matrix has

`det G_R = 21/65536`,

`rank G_R = 2`.

Thus the two frozen non-scalar Ricci/EOM-redundant algebraic cubic directions are genuinely independent in this TT/kinematic domain.

## Exact essentialized tensor

With the preregistered convention

`T_E_perp = T_E - c1 T_R1 - c2 T_R2`,

the exact coefficients are

`c1=-7/4`,

`c2=0`.

Therefore

`T_E_perp = T_E + (7/4) T_R1`.

Its exact norm is

`||T_E_perp||^2 = 45/1024 > 0`.

The residual overlaps are exactly

`<T_E_perp,T_R1>=0`,

`<T_E_perp,T_R2>=0`.

Define the frozen algebraic quotient projector

`P_E_alg[Gamma]=<T_E_perp,Gamma>/<T_E_perp,T_E>`.

Then exactly:

`P_E_alg[T_E]=1`,

`P_E_alg[T_R1]=0`,

`P_E_alg[T_R2]=0`.

All preregistered PASS controls are satisfied.

## Why c2=0 is nontrivial

The naive C3 template is not orthogonal to either redundant direction:

`<T_E,T_R1>/<T_E,T_E> = -7/19`,

`<T_E,T_R2>/<T_E,T_E> = 21/190`.

The exact zero coefficient of `T_R2` in the final essentialization does not mean `T_R2` was absent. It results from the nonzero correlation inside the redundant subspace:

`<T_R1,T_R2>=-1/128`.

After the `S^3` component is removed, the remaining C3 tensor is automatically orthogonal to the frozen `S S C` direction at this symmetric point.

Therefore one must solve the full redundant Gram system rather than subtract contaminations independently.

## Relation to SF050

SF050 proved that a naive Euclidean one-template C3 contraction is contaminated and cannot be promoted directly to physical matching.

SF051 now supplies the first explicit repair in a prospectively frozen subspace:

`NAIVE C3 TEMPLATE`
`-> ALGEBRAIC RICCI-REDUNDANT GRAM QUOTIENT`
`-> EXACT ESSENTIALIZED C3 TENSOR`.

This is genuine progress toward the off-shell embedding required by the dynamical FRG flow.

## Scope boundary

The PASS is intentionally narrow.

The complete sixth-derivative redundant sector also contains derivative structures generated by scale-dependent field redefinitions, including curvature-Laplacian / derivative-Ricci operators. Their full cubic fluctuation vertices are not represented by the pure `curvature^(1)^3` algebraic basis tested here.

Consequently SF051 does not yet establish a complete six-derivative physical projector.

The exact next missing object is:

`DERIVATIVE_REDUNDANT_COMPLETION_OF_ESSENTIAL_C3_FLUCTUATION_PROJECTOR`.

Only after that completion can the projector be inserted honestly into a dynamical FRG flow and tied to split/Nielsen/background-fluctuation identities.

## Updated theory chain

`SF049 PHYSICAL HELICITY PROJECTOR`
`-> SF050 EXPLICIT EUCLIDEAN C3 TEMPLATE + NAIVE-PROJECTOR COUNTEREXAMPLE`
`-> SF051 EXACT ALGEBRAIC REDUNDANT QUOTIENT PASS`
`-> DERIVATIVE-REDUNDANT COMPLETION`
`-> PROJECTED DYNAMICAL C3 FLOW`
`-> BACKGROUND/FLUCTUATION SPLIT-NIELSEN CONTROL`
`-> LORENTZIAN ON-SHELL LOW-ENERGY MATCHING`.

Retain:

`R_ASGS_TRUNCATION=1`.

`R_ASGS_PHYSICAL=UNDEFINED_MAP_NOT_CLOSED`.

## Claim ceiling

SF051 establishes no:

- complete six-derivative off-shell quotient;
- projected FRG C3 beta function;
- background/fluctuation equality;
- regulator-independent physical C3 coefficient;
- physical SF025 `b`;
- asymptotic-safety correctness;
- historical RCG-002 promotion;
- quantum `chi_ABC`;
- new physics or full quantum gravity.
