# SF028 — boundary-complete finite-time closed protocol — TERMINAL

Date: 2026-09-15
Preregistration commit: `dd7141e9a12dfa6b7ec01949527934d5559f4ab5`.

## RESULT / CLASSIFICATION

`INVALID_FROZEN_PROTOCOL_1D_POINT_GAUSSIAN_GRAVITATIONAL_OBJECT_SCOPED`

Secondary:

`BOUNDARY_COMPLETE_OBSERVABLE_CONSTRUCTION_REQUIRES_NEW_PREREGISTERED_3D_OR_FINITE_SIZE_GATE`

No connected coefficient from this frozen protocol is scientific authority.

## Decisive pre-computation falsifier

The preregistration froze all four motional bodies as normalized one-dimensional Gaussian wave packets while simultaneously using the exact point-particle Newtonian interaction

`V_N = -G sum_{I<J} m_I m_J/|x_I-x_J|`.

For any pair of independent nonzero-width 1D Gaussians, the relative coordinate `r=x_I-x_J` has a smooth Gaussian density `p(r)` with strictly positive value at `r=0`, regardless of how far apart the packet centers are.

Therefore the potential expectation contains, locally,

`integral_{-epsilon}^{epsilon} p(r)/|r| dr`.

Because `p(r)=p(0)+O(r)` with `p(0)>0`, the integral diverges logarithmically:

`integral_0^epsilon dr/r = infinity`.

Thus the frozen normalized state is not in the finite-expectation domain of the exact 1D point Newtonian potential used by the gate. The exponentially small collision-tail probability does not cure the divergence.

The same issue appears before any connected finite difference, before any Newtonian-versus-EIH comparison, and before any outcome-dependent choice.

## Why the protocol may not be silently repaired

The frozen protocol explicitly stated:

- one spatial dimension;
- point-particle Newton/EIH dynamics;
- nonzero Gaussian width;
- changing the physical object after outcome requires a new preregistration.

Replacing the state by a compact-support packet, softening the potential, adding a hard core, promoting the motional Hilbert space to three dimensions, or introducing finite-size mass profiles all change the mathematical object.

They are scientifically admissible repairs only in a new prospective gate.

## Why this is not a gravity-theory failure

The failure is a protocol/object-definition failure.

It does not imply:

- Newtonian gravity is inconsistent;
- EIH/GR is inconsistent;
- Gaussian wave packets are physically impossible;
- the SF026 classical trajectory result is invalid;
- the boundary-complete programme is impossible.

In three spatial dimensions, the local measure supplies `r^2 dr`, so the expectation of a `1/r` singularity is locally integrable (`r dr`). A 3D repair is therefore structurally natural and does not require softening Newtonian gravity.

## Controls retained

The failure is independent of:

- `G` coefficient tuning;
- branch geometry;
- apparatus distance `R`;
- connected outcome sign;
- 1PN coefficient;
- readout basis.

It is visible directly from the frozen state/operator pair.

## Terminal decision

The preregistered PASS condition requiring a mathematically well-defined normalized closed protocol is not satisfied.

Record:

`INVALID_FROZEN_PROTOCOL_1D_POINT_GAUSSIAN_GRAVITATIONAL_OBJECT_SCOPED`.

No `Theta3`, `Gamma3`, `Delta_1PN Theta3`, or `chi_ABC` value from this frozen protocol may be promoted.

## Exact next admissible gate

Open a new prospectively frozen repair:

`SF028B_3D_BOUNDARY_COMPLETE_COHERENCE_PROTOCOL_PREOUTCOME_GATE`.

Minimum repair requirements:

1. three-dimensional normalized motional packets with collinear branch centers;
2. the same single finite-mass recoil apparatus topology;
3. exact COM-closed controlled translations;
4. the same reduced-qubit-coherence/tomography readout concept;
5. point-particle Newton/EIH operators only after domain/integrability audit;
6. explicit finite-size applicability/effacement ceiling;
7. same-protocol Newtonian versus 1PN comparison;
8. no use of any exploratory SF028 connected coefficient.

## Claim ceiling

This terminal result is an object-definition falsification only. It establishes no connected signal, no measured phase, no 1PN discriminator, no quantum law, no new physics, and no quantum `chi_ABC`.