# SF052 — derivative-redundant completion of essential C3 fluctuation projector — PREOUTCOME

Date: 2026-09-15
Inherited authoritative front: SF051 `PASS_ALGEBRAIC_QUOTIENT_SCOPED`.

## HYPOTHESIS

The exact algebraic essentialized TT tensor from SF051 can be extended to the complete six-derivative redundant subspace of the audited minimal essential scheme at the same frozen Euclidean symmetric point by adding the derivative redundant action directions generated by the MES kernel. The resulting quotient may either remain nonzero (PASS scoped), collapse/degenerate (FAIL scoped), or be blocked if the cubic tensor object cannot be defined source-faithfully.

## EXACT OBJECT

Frozen essential tensor:

`T_E = T[C^3]`.

Frozen algebraic redundant tensors inherited from SF051:

`T_R1 = T[S_mu^nu S_nu^rho S_rho^mu]`,

`T_R2 = T[S^{mu nu} S^{rho sigma} C_{mu rho nu sigma}]`.

Frozen derivative redundant action directions from Baldazzi–Falls–Kluth–Knorr minimal essential scheme, eqs. (8)–(10):

`T_D1 = T[R Delta R]`,

`T_D2 = T[S_mu nu Delta S^{mu nu}]`,

with `Delta=-nabla^2`.

`nabla_mu nabla_nu R` is NOT added as an independent action direction because the audited MES source identifies the corresponding kernel term as a diffeomorphism whose tree-level redundant operator is outside the background-field approximation.

No other tensor is admitted post hoc.

## SOURCE AUTHORITY

Primary source: A. Baldazzi, K. Falls, Y. Kluth, B. Knorr, *Robustness of the derivative expansion in Asymptotic Safety*, arXiv:2312.03831, section II.2–II.3, especially eqs. (8)–(11).

The source gives the linearly independent sixth-order kernel basis and shows that the derivative action redundancies entering the classical side are `R Delta R` and `S Delta S`.

## FROZEN KINEMATICS / INNER PRODUCT

Same as SF050/SF051:

- `D=4` Euclidean flat background;
- `p1^2=p2^2=p3^2=1`;
- `p_i.p_j=-1/2` for `i!=j`;
- `p1+p2+p3=0`;
- five orthonormal symmetric traceless transverse polarization tensors for each leg;
- tensor inner product = exact sum over all `5^3=125` TT polarization triples;
- exact symbolic arithmetic where feasible.

## REQUIRED COMPUTATION

1. Construct the full cubic TT tensors generated by `R Delta R` and `S Delta S`, including all contributions required by the covariant operator through `O(h^3)` at the frozen point.
2. Verify Bose symmetry and frozen kinematics.
3. Compute the exact Gram matrix for `[C3,S3,SSC,RDeltaR,SDeltaS]` after removing identically zero directions.
4. Determine redundant-subspace rank.
5. Orthogonalize `T_C3` against the entire nonzero redundant subspace.
6. Record residual norm and normalized projector responses to every frozen redundant direction.

## POSITIVE CONTROLS

- Reproduce SF051 Gram entries for `[C3,S3,SSC]` exactly.
- Reproduce SF051 essentialized algebraic norm `45/1024` before derivative directions are added.
- If a derivative operator tensor is nonzero, direct contraction with its own basis must give positive norm.

## NEGATIVE CONTROLS

- `R Delta R` is tested, not assumed, for a vanishing pure-TT cubic tensor.
- Any exact zero direction is removed from the Gram inversion but retained as a zero-control result.
- The final normalized projector must return exact zero on every nonzero frozen redundant tensor admitted to the quotient.

## PASS

`PASS_COMPLETE_SIX_DERIVATIVE_TT_QUOTIENT_SCOPED` only if:

- every source-authorized derivative redundant direction is either explicitly shown to vanish on the frozen TT cubic domain or included in the redundant Gram space;
- the redundant Gram space is handled without post-hoc basis selection;
- a nonzero `T_C3_perp` survives;
- the normalized projector gives 1 on `C3` and 0 on all frozen nonzero redundant directions.

## FAIL

`C3_TT_DIRECTION_COLLAPSES_IN_COMPLETE_SIX_DERIVATIVE_REDUNDANT_QUOTIENT_SCOPED` if the source-authorized redundant span contains the frozen C3 tensor or the residual norm vanishes exactly.

## BLOCKED

`BLOCKED_DERIVATIVE_REDUNDANT_VERTEX_OBJECT_NOT_CLOSED` if the full covariant cubic tensor for a source-authorized derivative direction cannot be defined/constructed without adding unmotivated conventions or an incomplete surrogate.

## INVALID

Any post-result change to the derivative operator basis, kinematics, TT basis, inner product, or classification rules requires a new gate.

## INTERPRETATION CEILING

A PASS would close only the complete source-authorized six-derivative redundant quotient on the frozen flat-Euclidean TT symmetric-point cubic-vertex domain. It would NOT by itself provide a projected FRG beta function, background/fluctuation identity, regulator independence, Lorentzian amplitude, physical SF025 `b`, complete quantum law, historical RCG-002 authority, or quantum `chi_ABC`.
