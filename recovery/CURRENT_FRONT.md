# RQIRCGSF current authoritative front

Updated: 2026-09-15, after SF055A3 source TT-projector normalisation and SF055A3Q2 source external-symmetrisation normalisation; SF055A3Q1 exact projector-regrouping workflow remains non-terminal.

## Repository role / parent separation

`pppuu7-cmd/RQIRCG-structural-findings` remains an independent `PROSPECTIVE NEW-PRINCIPLE / SUCCESSOR SEARCH`.

Parent `pppuu7-cmd/RQIR-Candidate-Gravity` remains separately parked on

`WAIT_FOR_EXPLICIT_PROGRAMME_DISPOSITION_DECLARATION`.

No D1/D2/D3 disposition is selected. Successor results do not become historical RCG-002 authority without a separate prospective import/promotion gate.

## Retained theory authority

- SF021: `RHPI_SELECTED_AS_CLASSICAL_GRAVITATIONAL_LAW_PRINCIPLE_SCOPED`.
- SF025: `POST_RHPI_QUANTUM_COMPOSITION_DOES_NOT_FIX_FINITE_ON_SHELL_MATCHING_SCOPED`.
- SF043: `ASYMPTOTIC_SAFETY_PROVIDES_NONZERO_TRUNCATION_LEVEL_MATCHING_SELECTION_SCOPED`.
- `R_ASGS_TRUNCATION=1`.
- `R_ASGS_PHYSICAL=UNDEFINED_MAP_NOT_CLOSED`.
- SF046 coordinate bridge: `b = s G_C3/(32 pi G_N)` with unresolved global sign convention; no physical `b` selected.
- SF049: `PHYSICAL_ON_SHELL_C3_HELICITY_PROJECTOR_CONSTRUCTED_SCOPED`.
- SF052: `PASS_COMPLETE_SIX_DERIVATIVE_TT_QUOTIENT_SCOPED` only at the frozen D=4 Euclidean TT symmetric point.
- SF053: `BLOCKED_TARGET_FLOW_OBJECT_NOT_RECONSTRUCTIBLE_SCOPED` for previously published executed fluctuation truncations.
- SF054: `MINIMAL_C3_FLOW_REQUIRES_CORRELATED_3_4_5_VERTEX_INSERTIONS_SCOPED`.

Retain:

`QUANTUM_STATE_MEASURE_SELECTION != QUANTUM_LAW_ON_SHELL_MATCHING_SELECTION`.

`PHYSICAL_HELICITY_PROJECTOR != PROJECTED_FRG_FLOW`.

`FULL_MOMENTUM_DEPENDENCE_OF_A_SELECTED_TENSOR_DRESSING != FULL_TENSOR_INFORMATION`.

## SF055 projected fluctuation-FRG implementation contract

Parent preregistration:

`9d998d984566aa5bf290312a6a062fd632c85561`.

Status:

`ACTIVE / NON-TERMINAL`.

Frozen realization remains D=4 flat Euclidean, linear split, source-Fourier `partial_mu -> i p_mu`, harmonic/de-Donder Landau gauge, full six-dimensional Landau-transverse internal graviton space plus FP ghosts, source optimized regulator, symmetric external three-point kinematics, one common `g_C3^fluc` generating correlated `Gamma_C3^(3,4,5)`, and final target `B_C3=P_E_6d[partial_t Gamma^(3)_TT]` only after calibration.

No substantive C3 projected-flow output has been inspected or authorized.

### Lane B — terminal PASS scoped

`PASS_C3_COMMON_ORIGIN_VERTEX_GENERATOR_SCOPED`.

One covariant `int sqrt(g) C^3` generator reproduces `Gamma_C3^(2)=0`, all 125 SF052 cubic TT components with one common `3!` derivative factor, full Bose controls for n=3,4,5 and one common coupling.

Reproducibility run `34995827603`, artifact `10407234167`.

### Lane C — terminal PASS scoped

`PASS_IMPLEMENTED_THREE_POINT_FLOW_C3_INSERTION_MANIFEST_SCOPED`.

Source topology coefficients are retained as

`T5_GRAV=-1/2`, `B43_GRAV=+3`, `T333_GRAV=-3`, `T333_GHOST=+6`.

First-order common-C3 insertion histogram `{3:4,4:1,5:1}`; no C3 ghost or two-point insertion.

Run `34995935520`, artifact `10407288690`.

## Lane A — active EH/ghost baseline reproduction

Parent preregistration:

`prereg/SF055A3_SOURCE_FOURIER_BASELINE_LOOP_REPRODUCTION_PREOUTCOME.md`, freeze commit `e38e2fcc1a2da0c39be1d2cb28a7c8d5a3db276a`.

Frozen benchmark at k=1:

`g=1`, `mu_h=1/10`, `lambda_2=-1/20`, `lambda_3=-7/10`, `eta_h=eta_c=0`.

Frozen Eq. (14) derivative-at-p=0 targets:

`beta_g = 2 - (274830865/9179907)/pi`,

`beta_lambda3 = 7/5 - (3364922887/183598140)/pi`,

`beta_mu = -1/5 + (6554/3993)/pi`.

Hard-coding or fitting these targets is forbidden.

Retain source distinction:

`EQ14_ANALYTIC_TARGET != FINITE_DIFFERENCE_EQ10_TARGET`.

`THREE_POINT_FIGURE2_REPRODUCTION != BETA_MU_REPRODUCTION`.

A terminal Lane-A PASS still requires the three-point derivative/finite-difference implementation and an independent same-conventions two-point path for `beta_mu`.

### Source-Fourier seed — PASS

`PASS_SOURCE_FOURIER_EH_GHOST_BASELINE_SEED_ENGINE_SCOPED`.

Supplies source EH n=3/4/5 vertices, FP objects and source regulator path.

### A3.1 internal propagator/regulator — PASS

`PASS_A3_1_LANDAU_TRANSVERSE_INTERNAL_PROPAGATOR_CALIBRATION_SCOPED`.

For generic nonzero q, `dim K(q)=6`: five TT plus one independent transverse non-TT internal mode.

Retain:

`EXTERNAL_TT_PROJECTION != TT_ONLY_INTERNAL_PROPAGATION`.

### A3.2 routing — PASS

`PASS_A3_2_FIGURE2_ROUTING_PREFLIGHT_SCOPED`.

Run `35000570266`, artifact `10409661837`.

### A3.2 fixed-q contraction assembly — PASS

`PASS_A3_2_FIGURE2_TENSOR_CONTRACTION_ASSEMBLY_SCOPED`.

Uses full 6D internal graviton spaces, source-Fourier EH vertices, `G=(H+R)^-1`, canonical `S=G dotR G`, source ghost chain and source topology coefficients.

Run `35000997597`, artifact `10409825944`.

Retain:

`SINGLE_SCALE_PROPAGATOR = G dotR G`.

### Source TT projector normalisation — terminal PASS scoped

Merge authority:

`6c6a324ba94c25fa40dc30565b05407109a92ece`.

Classification:

`PASS_SF055A3_SOURCE_TT_PROJECTOR_NORMALISATION_SCOPED`.

Complete 125-component source tensor contractions give

`N_g^(-1)=0.00052874519051635`,

`N_g=1891.2701579818486`,

`N_lambda^(-1)=0.0026385724906858796`,

`N_lambda=378.99280900183135`.

The source Eq. (6) decomposition holds component-wise with maximum residual `1.734723475976807e-18`. The zero-momentum Lambda projector norm is stable under the prospectively frozen basis/global-rotation/symmetric-embedding controls.

Run `35001632950`, job `104491160831`, artifact `10409971299`, digest `sha256:e92c770acb56722729d307ee7740b01cf4d521b42c8dda65bd64d1e178b6a40d`.

These normalisations are frozen before any integrated baseline result and may not be rescaled to improve Eq. (14) agreement.

### SF055A3Q2 external symmetrisation normalisation — terminal PASS scoped

Prospective freeze:

`9e26ddae3d40d8a1e96330304b639947d0b3e681`.

Result:

`results/SF055A3Q2_EXTERNAL_SYMMETRISATION_NORMALISATION_TERMINAL.md`.

Classification:

`PASS_SOURCE_EXTERNAL_SYMMETRISATION_NORMALISATION_SCOPED`.

Exact three-labelled derivatives of `G=A^-1` produce, before the global bosonic `1/2`:

- one Gamma5 term with coefficient `-1`;
- six Gamma4*Gamma3 terms each `+1`;
- six Gamma3^3 terms each `-1`.

The FP supertrace gives `+1` per labelled ghost triangle.

Together with the frozen source Figure-2 coefficients `(-1/2,+3,-3,+6)`, this uniquely fixes

`SOURCE_SYM_3 = (1/6) SUM_OVER_S3_LABELLED_EXTERNAL_PERMUTATIONS`.

The unnormalised-sum interpretation fails the exact derivative combinatorics.

Source loop measure is fixed as

`int_q = int d^4q/(2 pi)^4`.

Run `35016518420`, job `104541230200`, artifact `10416150077`, digest `sha256:afa657ca7a4c8e70eff5013c91123c63ef352b025a63f1f2062edc7120538f85`.

No Eq. (14) value was used to select either factor.

## Active non-terminal implementation-equivalence workflow — SF055A3Q1

Prospective freeze:

`ebfced4bc837c3042eb58bd1f536b136d90c2ef4`.

Target classification if controls pass:

`PASS_SF055A3_EXACT_TT_PROJECTOR_REGROUPING_ACCELERATION_SCOPED`.

Exact identity under test:

`sum_abc T_abc F(E1_a,E2_b,E3_c) = sum_ab F(E1_a,E2_b, sum_c T_abc E3_c)`.

This is an exact regrouping only: no SVD truncation, stochastic estimator, fitted rescaling or target-dependent approximation is authorized.

Authoritative workflow:

- run `35016211111`;
- job `104540163228`;
- current status at this recovery update: `NON_TERMINAL / IN_PROGRESS`.

Do not create a competing Q1 verdict or use partial values from this workflow.

If Q1 passes, its grouped projector may replace the 125-term brute-force projector during quadrature only after terminal authority. If Q1 fails, retain the brute-force complete TT projector; do not relax the equivalence tolerance.

## Current exact frontier

SF055 Lane B: terminal PASS scoped.

SF055 Lane C: terminal PASS scoped.

SF055 Lane A: **OPEN**.

Immediate sequence:

1. terminalize authoritative SF055A3Q1 when run `35016211111` is terminal;
2. use either the validated exact grouped projector or the retained brute-force projector;
3. execute source-normalized Figure-2 integration with
   `Sym_3=(1/6)sum_S3` and `int d^4q/(2pi)^4`;
4. obey the already-frozen deterministic Gauss-Legendre convergence sequence `N={8,12,16,24}` and its unchanged convergence thresholds;
5. implement both the finite-difference Eq. (10) architecture and the derivative-at-p=0 Eq. (14) benchmark without equating them;
6. independently reconstruct the same-conventions two-point flow required for `beta_mu`;
7. only if all frozen Lane-A targets/controls pass may SF055 be considered for terminal implementation validation.

`SF055_TERMINAL_PASS = FALSE`.

No substantive projected C3 beta output may be inspected before this calibration chain terminalizes.

## Operational front retained

SF028B remains the boundary-complete coherence authority; SF029-SF042 retain the known-physics calibration/control chain.

Operational blocker remains:

`DEVICE_LEVEL_NONLINEAR_CROSSTALK_MAGNITUDE_AND_STABILITY_DATA_REQUIRED`.

Retain:

`OPEN_ACTION != MEASURED_REDUCED_COHERENCE`.

`NONZERO_CONNECTED_SIGNAL != NEW_THREE_BODY_GRAVITATIONAL_VERTEX`.

`STRUCTURAL_CONTROL_CLOSURE != DEVICE_VALIDATION`.

## Claim locks

Without separate authority:

- no physical SF025 `b` selected;
- no truncation constant promoted to physical matching;
- no background/fluctuation equality assumed;
- no frozen-point quotient PASS promoted to arbitrary kinematics;
- no projected dynamical C3 beta function claimed;
- no sub-lane PASS promoted to SF055 terminal PASS;
- no Euclidean off-shell coefficient called a physical observable without continuation;
- no operational result selects quantum-law matching;
- no quantum `chi_ABC` computed;
- no historical RCG-002 authority changed;
- no asymptotic-safety correctness claim;
- no `NEW_PHYSICS_FOUND`, `FULL_QUANTUM_GRAVITY`, `THEORY_ESTABLISHED`, or unique quantum-gravity selector claim.
