# RQIRCG structural findings

Status: **PROSPECTIVE NEW-PRINCIPLE SEARCH**  
Scientific lock: **`chi_ABC` prediction is embargoed until a new physical principle is prospectively selected.**

This repository is a separate research branch seeded only by frozen structural findings from `RQIR-Candidate-Gravity`. It is **not** an in-place repair of RCG-002 and is not `RQIR-Candidate-Gravity-v2`.

## Research question

What is the minimal independently motivated physical principle that removes the nonlinear underdetermination exposed by RQIRCG, while preserving the validated relational controlled-phase and weak-field structure?

Target chain:

`RQIRCG structural findings`
→ `new physical principle search`
→ `prospectively selected principle`
→ `nonlinear dynamics`
→ `chi_ABC prediction`
→ `GR / semiclassical / EFT discrimination`

## Authoritative imports

Only the following frozen objects may enter as established starting structure:

1. frozen RQIR requirements as external judge;
2. RCG-002 operational relational controlled-phase architecture;
3. connected observable
   `chi_ABC = Delta_A Delta_B Delta_C phi`;
4. G97 closed total-source preparation bookkeeping;
5. the exact pairwise-baseline theorem `Delta_A Delta_B Delta_C W_2 = 0`;
6. NP1 connected-channel nonuniqueness;
7. CM1 closed-mediator nonuniqueness;
8. VB1 current-version boundary: present principles do not entail nonlinear dynamics.

## Quarantine: not physics of this repository

The following are controls, comparators, or historical diagnostics only and cannot be promoted into the model without an independently selected principle:

- free `lambda`, `gamma`, or any fitted connected coefficient;
- the specific CM1 mediator;
- arbitrary cubic or higher kernels such as an inserted `K_3`;
- dynamics imported from QGR, MSQGR, GR, semiclassical gravity, stochastic gravity, or EFT;
- any preselected nonzero value or sign of `chi_ABC`;
- post-VB1 principle candidates from the old repository, including V2P1/RSC, except as negative diagnostic history.

## Prospective rule

A new principle must be selected **before** any calculation of the connected three-source prediction. If no candidate passes the frozen selection gate, the correct result is `NO_PRINCIPLE_SELECTED`, not a patched nonlinear model.

The first gate is `SF001`: search for a minimal principle capable of turning the closed relational source/history system into a unique nonlinear generator without an independent connected coupling.
